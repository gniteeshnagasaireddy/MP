import React from 'react';
export default function About(){
  return (
    <div>
      <div className="card">
        <h2>About</h2>
        <p className="small">This project demonstrates a simple full-stack realtime weather tracker for India. Data sources: OpenWeatherMap (default). Swap to IMD or other providers by changing the backend fetch logic.</p>
      </div>
    </div>
  )
}
